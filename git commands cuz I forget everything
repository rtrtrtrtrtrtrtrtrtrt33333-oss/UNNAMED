git add .
git commit -m "update"
git push

Поменять владейльце или чего типо того
git config --global --add safe.directory "C:/Users/main/Desktop/Nest/HTML/Unnamed project"

history reset:
git status

git checkout --orphan new-main

git add -A

git commit -m "Initial project version"

git log --oneline

git branch

git branch -D main

git branch -m main

git status
git log --oneline